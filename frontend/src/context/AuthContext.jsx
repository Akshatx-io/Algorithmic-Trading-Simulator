/**
 * DEPRECATED — Phase 2.1 (audit finding 3.11).
 *
 * Auth state has moved to a Zustand store. Import from:
 *
 *     import { useAuthStore } from "../store/authStore";
 *     import useAuth from "../hooks/useAuth";
 *
 * This file is preserved as a deprecation marker only. It should be removed
 * via `git rm frontend/src/context/AuthContext.jsx` once no imports remain
 * (`scripts/cleanup_local.sh` will do this on the next run).
 */
