# Frontend environment template.
# Copy to .env.local and edit. Both .env.local and .env are gitignored.

# REST API base URL (must include /api/v1 suffix).
VITE_API_URL=http://localhost:8000/api/v1

# WebSocket URL.
VITE_WS_URL=ws://localhost:8000/api/v1/ws
